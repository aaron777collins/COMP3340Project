export type ItemModel = {
    name: string;
    price: number;
    rating: number;
    description: string;
}

export type CartItemModel = {
    name: string;
    price: number;
    description: string;
    rating: number;
    quantity: number;
}